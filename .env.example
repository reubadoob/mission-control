# ====================================
# Mission Control Configuration
# ====================================

# Database path (default: ./mission-control.db)
DATABASE_PATH=./mission-control.db

# ====================================
# OpenClaw Gateway Configuration
# ====================================

# OpenClaw Gateway URL (default: ws://127.0.0.1:18789)
# For remote connections via Tailscale: wss://your-machine.tail12345.ts.net
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789

# OpenClaw Gateway authentication token (required for remote connections)
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# GearSwitchr internal context API key (used to enrich dispatch prompts)
INTERNAL_CONTEXT_API_KEY=your_internal_context_api_key_here

# Discord relay controls
OPENCLAW_DISCORD_RELAY_ENABLED=true
OPENCLAW_DISCORD_CHANNEL_ID=[PLACE YOUR DISCORD CHANNEL ID HERE]

# Discord -> Mission Control task command controls
# Command format: !task <title> | <description>
OPENCLAW_DISCORD_TASK_COMMANDS_ENABLED=false
# Optional: comma-separated Discord user IDs allowed to create tasks
OPENCLAW_DISCORD_TASK_COMMAND_USER_ALLOWLIST=
# Workspace where Discord-created tasks will be inserted
OPENCLAW_DISCORD_TASK_WORKSPACE_ID=default
# Default priority applied to Discord-created tasks (low|normal|high|urgent)
OPENCLAW_DISCORD_TASK_DEFAULT_PRIORITY=normal
# Guardrail: reject command if open task count in workspace reaches this threshold
OPENCLAW_DISCORD_TASK_MAX_OPEN=200
# Guardrail: minimum interval between commands from same sender (ms)
OPENCLAW_DISCORD_TASK_MIN_INTERVAL_MS=5000
# Enable DM task intake via agent:main:discord:dm:<userId> sessions
DISCORD_TASK_DM_ENABLED=false
# Optional: mirror DM task creation notifications to a channel id
DISCORD_TASK_DM_AUDIT_CHANNEL=

# ====================================
# Planning Configuration
# ====================================

# Planning timeout in milliseconds (default: 30000 = 30 seconds)
# How long to wait for Charlie to respond to a planning question
PLANNING_TIMEOUT_MS=30000

# Planning poll interval in milliseconds (default: 2000 = 2 seconds)
# How frequently to poll for updates from OpenClaw during planning
PLANNING_POLL_INTERVAL_MS=2000

# ====================================
# Workspace Configuration
# ====================================

# Mission Control API URL (auto-detected if not set)
# Override for remote access or custom deployments
# MISSION_CONTROL_URL=http://localhost:4000

# Base workspace directory (default: ~/Documents/Shared)
# All Mission Control files are stored here
# WORKSPACE_BASE_PATH=~/Documents/Shared

# Projects directory (default: ~/Documents/Shared/projects)
# Each project gets a unique folder here
# PROJECTS_PATH=~/Documents/Shared/projects

# Git repository root used for isolated task worktrees (required for worktree prep/dispatch)
GIT_REPO_ROOT=

# Base branch used when creating new worktree branches
WORKTREE_BASE_BRANCH=main

# ====================================
# Security Configuration
# ====================================

# API authentication token (required for production)
# If not set, authentication is DISABLED (local dev mode only)
# Generate with: openssl rand -hex 32
MC_API_TOKEN=

# Webhook signature secret for agent-completion webhook
# If not set, signature validation is DISABLED (local dev mode only)
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=

# ====================================
# Security Notes
# ====================================
#
# NEVER commit this file with real values!
# This is a template - copy to .env.local and configure:
#
#   cp .env.example .env.local
#
# Then edit .env.local with your actual values.
# .env.local is gitignored and will not be committed.
